import { LanguageChipsPipe } from './language-chips.pipe';

describe('LanguageChipsPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageChipsPipe();
    expect(pipe).toBeTruthy();
  });
});
